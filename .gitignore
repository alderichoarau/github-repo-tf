# Terraform files not to be version-controlled
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
*.tfplan

# Sensitive variables (contains the GitHub token!)
terraform.tfvars

# Claude Code local settings
.claude/

# Local collaborators list (to be copied into GitHub Actions Variables)
collaborators.txt

# macOS files not to be version-controlled
.DS_Store