name: Terraform PR Checks

on:
  pull_request:
    branches: [main]
    paths:
      - "**.tf"
      - ".github/workflows/**.yml"

jobs:
  # -------------------------------------------------------
  # 1. Format + Validate
  # -------------------------------------------------------
  lint:
    name: Format & Validate
    runs-on: ubuntu-latest

    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_API_TOKEN }}
      TF_VAR_github_token: "dummy"
      TF_VAR_github_owner: "dummy"
      TF_VAR_repo_name: "dummy"

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "~> 1.9"

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v6

      - name: TFLint
        run: |
          tflint --init
          tflint --format compact

  # -------------------------------------------------------
  # 2. Security — Checkov
  # -------------------------------------------------------
  checkov:
    name: Checkov Security Scan
    runs-on: ubuntu-latest

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: terraform
          output_format: cli,sarif
          output_file_path: console,results.sarif
          soft_fail: true  # does not block the PR, only displays results

      - name: Upload SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: results.sarif

  # -------------------------------------------------------
  # 3. Terraform Plan (preview changes)
  # -------------------------------------------------------
  plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: lint

    permissions:
      contents: read
      pull-requests: write

    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_API_TOKEN }}
      TF_VAR_github_owner: ${{ secrets.GH_OWNER }}
      TF_VAR_github_token: ${{ secrets.GH_TOKEN }}
      TF_VAR_gitignore_template: "Terraform"
      TF_VAR_repo_name: "pr-preview"
      TF_VAR_repo_description: "Preview from PR #${{ github.event.pull_request.number }}"
      TF_VAR_collaborators: "{}"

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "~> 1.9"

      - name: Terraform Init
        run: terraform init

      # Use a workspace scoped to this PR so concurrent PR plans never
      # collide with each other or with a real repo-factory workspace.
      - name: Select or create workspace
        run: terraform workspace select -or-create "pr-preview-${{ github.event.pull_request.number }}"

      - name: Terraform Plan
        run: terraform plan -no-color 2>&1 | tee plan.txt

      - name: Post Plan as PR comment
        uses: actions/github-script@v9
        if: always()
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('plan.txt', 'utf8');
            const truncated = plan.length > 60000 ? plan.slice(0, 60000) + '\n...(truncated)' : plan;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Terraform Plan\n\`\`\`hcl\n${truncated}\n\`\`\``
            });